package se.lexicon.view;

/**
 * The view (Step 1 scaffolding — the MVC split is Step 2 in the series).
 *
 * Role in the fuller architecture:
 * - the ONLY class allowed to touch the terminal (Scanner / System.out)
 * - displays menus, lists books, prompts for input
 * - contains zero business logic — it only reads and prints
 * - reports errors as friendly messages (never stack traces)
 *
 * Empty in Step 1 on purpose: today is all about the {@code model} layer.
 */
public class BookView {
}