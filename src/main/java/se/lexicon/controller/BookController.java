package se.lexicon.controller;

import se.lexicon.view.BookView;

/**
 * The controller (Step 1 scaffolding — the MVC split is Step 2 in the series).
 *
 * Role in the fuller architecture:
 * - owns the flow of the program (menu loop, which action runs next)
 * - reads user input via the {@link BookView}
 * - calls the service/DAO to do the actual work
 * - tells the view what to print
 *
 * It is intentionally EMPTY in Step 1: the model layer is the only thing you
 * build today. Fill it in during Step 2.
 */
public class BookController {
}