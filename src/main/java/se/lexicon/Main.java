package se.lexicon;

import se.lexicon.model.Book;

/**
 * Workshop 1 entry point — demonstrates the {@link Book} model.
 *
 * Task 3: create a few books, print them, and show that invalid input
 * throws IllegalArgumentException (unchecked, so no catch is required).
 */
public class Main {
    public static void main(String[] args) {

        // Scenario 1: a valid book is available by default
        // Scenario 5: the 4-arg constructor can mark a book as borrowed
        // Scenario 3: an ISBN that is not 13 digits is rejected
        // Scenario 2: a blank title is rejected
        // Bonus: two books with the same ISBN are "the same book"
    }
}